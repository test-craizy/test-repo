name: 🚀 AKS Workflow Execution

on:
  workflow_dispatch:

jobs:
  call-aks-template:
    uses: test-craizy/DevSecOps-scripts/.github/workflows/workflow-aks-template.yml@main
    with:
      componente: "backend-service"

resources:
  repositories:
    - repository: DevSecOps-scripts
      type: git
      name: test-craizy/DevSecOps-scripts
      ref: main